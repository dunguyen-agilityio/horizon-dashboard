import { Skeleton } from '@nextui-org/skeleton';

const CheckTableSkeleton = () => (
  <Skeleton className="flex-auto 2xl:flex-1 h-[350px] bg-white dark:bg-indigo-light rounded-md" />
);

export default CheckTableSkeleton;
