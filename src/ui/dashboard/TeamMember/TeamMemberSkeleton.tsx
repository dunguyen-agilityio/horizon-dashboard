import { Skeleton } from '@nextui-org/skeleton';

const TeamMemberSkeleton = () => (
  <Skeleton className="flex-1 h-full rounded-md bg-white dark:bg-indigo-light" />
);

export default TeamMemberSkeleton;
